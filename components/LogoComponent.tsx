import { Text } from '@chakra-ui/layout';
import React from 'react';

interface LogoComponentProps {}

export const LogoComponent: React.FC<LogoComponentProps> = ({}) => {
  return <Text fontSize="3xl">Fit Tracker</Text>;
};
